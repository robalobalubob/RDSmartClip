<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/combined_chart_scroll_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:clipToPadding="false"
    android:paddingTop="?attr/actionBarSize"
    android:paddingBottom="?attr/actionBarSize">
 <!-- change to row / column layout -->
    <LinearLayout
        android:id="@+id/combined_chart_linear_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Voltage Chart -->
        <com.github.mikephil.charting.charts.LineChart
            android:id="@+id/voltage_chart"
            android:layout_width="match_parent"
            android:layout_height="300dp" />

        <!-- Current Chart -->
        <com.github.mikephil.charting.charts.LineChart
            android:id="@+id/current_chart"
            android:layout_width="match_parent"
            android:layout_height="300dp" />

        <!-- Acceleration Chart -->
        <com.github.mikephil.charting.charts.LineChart
            android:id="@+id/acceleration_chart"
            android:layout_width="match_parent"
            android:layout_height="300dp" />

        <!-- RPM Chart -->
        <com.github.mikephil.charting.charts.LineChart
            android:id="@+id/rpm_chart"
            android:layout_width="match_parent"
            android:layout_height="300dp" />

        <!-- Add more charts if needed -->

    </LinearLayout>
</ScrollView>
